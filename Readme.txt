** Intrucoes para upload/git

Abrir o power terminal do windows (pelo GitWin ou GitHub Desktop nao funciona)

git add .
git commit -m "update"
git push origin master
git lfs push --all origin master

